spring:
  application:
    name: optical-ai-lab
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}
      chat:
        options:
          model: claude-opus-4-8
          max-tokens: 8000

server:
  port: 8080
